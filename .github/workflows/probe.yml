name: Gate Probe (public repo)
on:
  push:
jobs:
  hosted:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hosted runner OK — no billing gate"
  selfhosted:
    runs-on: self-hosted
    steps:
      - run: echo "self-hosted OK — no billing gate"
